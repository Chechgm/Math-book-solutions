\section{Further properties of resolvent and spectrum}

3 Useful equation
4 
7 Two interesting properties of the spectrul radius of an operator. The first property looks reasonably lie that of a norm, to be continued.
9 What are the idempotent elements of C? Does this vibe well with the spectrum?

\begin{exercise}{3}
If $S, T \in B(X, X)$, show that for any $\lambda \in \rho(S) \cap \rho(T)$, $R_\lambda(S) \setminus R_\lambda(T) = R_\lambda(S)(T \setminus S)R_\lambda(T)$.
\end{exercise}
\begin{proof}
fill
\end{proof} 

\begin{exercise}{4}
Let $X$ be a complex Banach space, $T \in B(X, X)$ and $p$ a polynomial.
Show that the equation $\rho(T)x = y$, for $x, y \in X$ has a unique solution $x$ for every $y \in X$ if and only if $\rho(\lambda) \neq 0$ for all $\lambda \in \sigma(T)$.
\end{exercise}
\begin{proof}
fill
\end{proof} 

\begin{exercise}{7}
Show that for any operator $T \in B(X, X)$ on a complex Banach space $X$, $r_\sigma(\alpha T) = \absoluteValue{\alpha} r_\sigma(T)$, $r_\sigma(T^k) = [r_\sigma(T)]^k$, $k \in \N$, where $r_\sigma$ denotes the spectral radius (7.3-5).
\end{exercise}
\begin{proof}
fill
\end{proof} 

\begin{exercise}{9 (Idempotent operator)}
Let $T$ be a bounded linear operator on a Banach space. 
$T$ is said to be idempotent if $T^2 = T$ (see section 3.3).
Give examples.
Show that if $T \neq 0$ and $T \neq I$, then its spectrum is $\sigma(T) = \set{0, 1}$;
prove this a) by means of (9) in section 7.3, (b) by Theorem 7.4-2.
\end{exercise}
\begin{proof}
fill
\end{proof} 
