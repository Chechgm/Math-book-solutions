\section{Spectral properties of compact linear operators on normed spaces}

2 This shows that the set of compact operators is a Banach algebra
3 Nice structure of compact operators which is reminiscent of the finite dimensional case. Sadly, I have looked hard in Kreyszig for a spectal theorem for compact self-adjoint operators and didn't really find anything. The idea would be that they would indeed have a "basis" of eigenvectors, but Kreyszig doesn't seem to have this. Disappointing. We will do it later on as an exercise then 
4 This shows that compact operators form a two-sided ideal
6
7 In usual numbers: If an element is infinitesimal, then its complex conjugate and its modulus is too. Obviously. 
8 Notice that this exercise specifies infinite dimensional space and not Banach space. In fact, this entire exercise is vacuous in case of a Banach space. There simply cannot be a compact linear operator on a Banach space whose inverse is defined on all of X. Why not?
14 Important exercise that shows that in infinite dimensions, 0 is always in the spectrum. 0 also needs not be an eigenvalue. It is the only point in the spectrum which might not be an eigenvalue of T. The others all have to be.
15 

\begin{exercise}{2}
Let $X, Y$ and $Z$ be normed spaces, and let $T: X \to Y$ and $S: Y \to Z$.
If $T$ and $S$ are compact linear operators, show that $S T: X \to Z$ is a compact linear operator.
\end{exercise}
\begin{proof}
fill
\end{proof} 

\begin{exercise}{3}
If $T$ is a compact linear operator, show that for any given number $k > 0$ there are at most finitely many liearly independent eigenvectors of $T$ corresponding to eigenvalues of absolute value greater than $k$.
\end{exercise}
\begin{proof}
fill
\end{proof} 

\begin{exercise}{4}
Let $T_j: X_j \to X_{j+1}$, $j = 1, 2, 3$ be bounded linear operators on normed spaces.
If $T_2$ is compact, show that $T = T_3 T_2 T_1: X_1 \to X_4$ is compact.
\end{exercise}
\begin{proof}
fill
\end{proof} 

\begin{exercise}{6}
Let $H$ be a Hilbert space, $T: H \to H$ is a bounded linear operator and $T^*$ the Hilbert-adjoint operator of $T$.
Show that $T$ is compact if and only if $T^* T$ is compact.
\end{exercise}
\begin{proof}
fill
\end{proof} 

\begin{exercise}{7}
If $T$ in exercise 6 is compact, show that $T^*$ is compact.
\end{exercise}
\begin{proof}
fill
\end{proof} 

\begin{exercise}{8}
If a compact linear operator $T: X \to X$ on an infinite dimensional normed space $X$ has an inverse which is defined on all of $X$, show that the inverse cannot be bounded.
\end{exercise}
\begin{proof}
fill
\end{proof} 

\begin{exercise}{14}
Let $T: X \to X$ be a compact linear operator on a normed space.
If $\dim X = \infty$ show that $0 \in \sigma(T)$.
\end{exercise}
\begin{proof}
fill
\end{proof} 

\begin{exercise}{15}
Let $T: \ell^2 \to \ell^2$ be defined by $y = (y_i) = T x$, $x = (x_i)$, $y_{2k} = x_{2k}$, and $y_{2k - 1} = 0$, where $k = 1, 2 \dots$.
Find $\cN(T_\lambda^n)$.
Is $T$ compact?
\end{exercise}
\begin{proof}
fill
\end{proof} 
